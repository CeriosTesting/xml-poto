{
	"name": "xml-poto",
	"version": "0.0.0",
	"private": true,
	"author": "Ronald Veth - Cerios",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "npm run --workspaces build",
		"changeset": "changeset",
		"compile": "npm run --workspaces compile",
		"deps:check": "syncpack lint",
		"deps:fix": "syncpack fix",
		"deps:update": "npx npm-check-updates --workspaces -u --dep prod,dev,optional,peer,packageManager --install never && npm install --workspaces",
		"format": "oxfmt",
		"format:check": "oxfmt --check",
		"lint": "oxlint --type-aware",
		"lint:fix": "oxlint --type-aware --fix",
		"pack": "npm run --workspaces pack",
		"release": "changeset publish",
		"test": "npm run --workspaces test",
		"version-packages": "changeset version",
		"version-packages:prepare-release": "npm run version-packages && npm run format"
	},
	"devDependencies": {
		"@changesets/cli": "^3.0.1",
		"esbuild": "^0.28.2",
		"husky": "^9.1.7",
		"lint-staged": "^17.4.1",
		"npm-check-updates": "^23.1.0",
		"oxfmt": "^0.65.0",
		"oxlint": "^1.80.0",
		"oxlint-tsgolint": "^7.0.2001",
		"syncpack": "^15.3.3",
		"typescript": "^7.0.2",
		"vitest": "4.1.11"
	},
	"engines": {
		"node": ">=20.19.0"
	},
	"allowScripts": {
		"esbuild@0.28.1": true,
		"fsevents@2.3.3": true
	}
}
