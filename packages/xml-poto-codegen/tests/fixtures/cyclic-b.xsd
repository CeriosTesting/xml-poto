<?xml version="1.0" encoding="UTF-8"?>
<!-- Includes cyclic-a.xsd back, forming the cycle the parser must not recurse into. -->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema">

	<xs:include schemaLocation="cyclic-a.xsd"/>

	<xs:complexType name="BType">
		<xs:sequence>
			<xs:element name="Code" type="xs:string"/>
		</xs:sequence>
	</xs:complexType>

</xs:schema>
